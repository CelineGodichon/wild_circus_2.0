<nav class="navbar navbar-expand-lg bg-dark">
    <a class="navbar-brand title-navbar" href="{{ path('homepage') }}">
        <h1>WILD FREAK CIRCUS</h1>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon burger-icon">
            <i class="fab fa-optin-monster"></i>

        </span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <div class="navbar-nav d-flex justify-content-between">
            <div class="d-flex nav-list">
                <div class="nav-item">
                    <a class="nav-link" href="{{ path('performances') }}">Performances</a>
                </div>
                <div class="nav-item">
                    <a class="nav-link" href="{{ path('artists') }}">Monsters</a>
                </div>
                <div class="nav-item">
                    <a class="nav-link" target="_blank"
                       href="https://www.google.com/search?q=animal+cruelty+circus&client=ubuntu&hs=MuY&channel=fs&sxsrf=ACYBGNQBHuSQCN6ISJYfufed7B7nnuN4sw:1580315598458&source=lnms&tbm=isch&sa=X&ved=2ahUKEwjq74OvnqnnAhWwAmMBHX3SA70Q_AUoAXoECAsQAw&biw=1600&bih=748#imgrc=03ya34nS4_giiM">Animals</a>
                </div>
            </div>
            <div class="d-flex nav-list">
                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <div class="nav-item">
                            <a class="nav-link" href="{{ path('admin_boardtable') }}"> Boardtable</a>
                        </div>
                    {% else %}
                        <div class="nav-item">
                            <a href="{{ path('profile', { user : app.user.id}) }}" class="nav-link">Profile</a>
                        </div>
                    {% endif %}
                    <div class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                    </div>

                {% else %}
                    <div class="nav-item">
                        <a class="nav-link {{ app.request.get('_route') == 'app_login'? 'active' : '' }}"
                           href="{{ path('app_login') }}">
                            Login
                        </a>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</nav>


